# AGENTS.md

## Scope

These instructions apply to files under `public/resume/`.

## Policy

- This directory is read-only for agents by default.
- Agents may read files in this directory when needed for context.
- Do not modify, create, delete, rename, or regenerate files in this directory unless the user explicitly requests work in `public/resume/`.
- If a task can be completed outside `public/resume/`, do that instead.
