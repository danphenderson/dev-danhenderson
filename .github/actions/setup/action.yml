name: Setup Node environment
description: Set up Node 20 with npm cache and install dependencies

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: npm

    - run: npm install -g npm@11
      shell: bash

    - run: npm ci
      shell: bash
